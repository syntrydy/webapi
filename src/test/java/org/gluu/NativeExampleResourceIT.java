package org.gluu;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeExampleResourceIT extends HealthResourceTest {

    // Execute the same tests but in native mode.
}